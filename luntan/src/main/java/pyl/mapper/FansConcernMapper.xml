<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://www.tablego.cn
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author: bianj
    Email: tablego@qq.com
    Version: 6.0.0
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- FANS_CONCERN -->
<mapper namespace="pyl.mapper.fansConcern">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="fansConcernMap" type="FansConcern">
        <id column="FANS_CONCERN_ID" property="fansConcernId" jdbcType="INTEGER" />
        <result column="FANS_UID" property="fansUid" jdbcType="INTEGER" />
        <result column="FANS_UEAMIL" property="fansUeamil" jdbcType="VARCHAR" />
        <result column="FANS_UNICKNAME" property="fansUnickname" jdbcType="VARCHAR" />
        <result column="CONCERN_UID" property="concernUid" jdbcType="INTEGER" />
        <result column="CONCERN_UEAMIL" property="concernUeamil" jdbcType="VARCHAR" />
        <result column="CONCERN_UNICKNAME" property="concernUnickname" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        FANS_CONCERN_ID, FANS_UID, FANS_UEAMIL, FANS_UNICKNAME, CONCERN_UID, CONCERN_UEAMIL, CONCERN_UNICKNAME
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findFansConcernAll" resultMap="fansConcernMap">
        SELECT
        <include refid="allColumns" />
        FROM FANS_CONCERN
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findFansConcernByCondition" resultMap="fansConcernMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM FANS_CONCERN WHERE 1 = 1
        <if test="fansUid != null">
            AND FANS_UID = #{fansUid}
        </if>
        <if test="fansUeamil != null and fansUeamil != ''">
            AND FANS_UEAMIL LIKE CONCAT('%', #{fansUeamil}, '%')
        </if>
        <if test="fansUnickname != null and fansUnickname != ''">
            AND FANS_UNICKNAME LIKE CONCAT('%', #{fansUnickname}, '%')
        </if>
        <if test="concernUid != null">
            AND CONCERN_UID = #{concernUid}
        </if>
        <if test="concernUeamil != null and concernUeamil != ''">
            AND CONCERN_UEAMIL LIKE CONCAT('%', #{concernUeamil}, '%')
        </if>
        <if test="concernUnickname != null and concernUnickname != ''">
            AND CONCERN_UNICKNAME LIKE CONCAT('%', #{concernUnickname}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findFansConcernByIds" resultMap="fansConcernMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM FANS_CONCERN WHERE FANS_CONCERN_ID IN
        <foreach collection="list" index="index" item="fansConcernId" open="(" separator="," close=")">
            #{fansConcernId}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertFansConcern" parameterType="map">
        INSERT INTO FANS_CONCERN (
            FANS_CONCERN_ID, FANS_UID, FANS_UEAMIL, FANS_UNICKNAME, CONCERN_UID, CONCERN_UEAMIL, CONCERN_UNICKNAME
        ) VALUES (
            #{fansConcernId},
            #{fansUid},
            #{fansUeamil},
            #{fansUnickname},
            #{concernUid},
            #{concernUeamil},
            #{concernUnickname}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateFansConcern" parameterType="map">
        UPDATE FANS_CONCERN
        <set>
            <if test="fansUid != null">
                FANS_UID = #{fansUid},
            </if>
            <if test="fansUeamil != null">
                FANS_UEAMIL = #{fansUeamil},
            </if>
            <if test="fansUnickname != null">
                FANS_UNICKNAME = #{fansUnickname},
            </if>
            <if test="concernUid != null">
                CONCERN_UID = #{concernUid},
            </if>
            <if test="concernUeamil != null">
                CONCERN_UEAMIL = #{concernUeamil},
            </if>
            <if test="concernUnickname != null">
                CONCERN_UNICKNAME = #{concernUnickname}
            </if>
        </set>
        WHERE FANS_CONCERN_ID = #{fansConcernId}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteFansConcernById" parameterType="int">
        DELETE FROM FANS_CONCERN WHERE FANS_CONCERN_ID = #{fansConcernId}
    </delete>
    
</mapper>