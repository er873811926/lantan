<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://www.tablego.cn
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author: bianj
    Email: tablego@qq.com
    Version: 6.0.0
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 1表示 回复(USER_MESSAGE) -->
<mapper namespace="pyl.mapper.userMessage">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="userMessageMap" type="UserMessage">
        <id column="MESSAGE_ID" property="messageId" jdbcType="INTEGER" />
        <result column="U_ID" property="uId" jdbcType="INTEGER" />
        <result column="U_EAMIL" property="uEamil" jdbcType="VARCHAR" />
        <result column="POSTS_NO" property="postsNo" jdbcType="VARCHAR" />
        <result column="POSTS_TITLE" property="postsTitle" jdbcType="VARCHAR" />
        <result column="OTHER_U_EMAIL" property="otherUEmail" jdbcType="VARCHAR" />
        <result column="MESSAGE_CONTENT" property="messageContent" jdbcType="VARCHAR" />
        <result column="TYPE" property="type" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        MESSAGE_ID, U_ID, U_EAMIL, POSTS_NO, POSTS_TITLE, OTHER_U_EMAIL, MESSAGE_CONTENT, TYPE
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findUserMessageAll" resultMap="userMessageMap">
        SELECT
        <include refid="allColumns" />
        FROM USER_MESSAGE
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findUserMessageByCondition" resultMap="userMessageMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM USER_MESSAGE WHERE 1 = 1
        <if test="uId != null">
            AND U_ID = #{uId}
        </if>
        <if test="uEamil != null and uEamil != ''">
            AND U_EAMIL LIKE CONCAT('%', #{uEamil}, '%')
        </if>
        <if test="postsNo != null and postsNo != ''">
            AND POSTS_NO LIKE CONCAT('%', #{postsNo}, '%')
        </if>
        <if test="postsTitle != null and postsTitle != ''">
            AND POSTS_TITLE LIKE CONCAT('%', #{postsTitle}, '%')
        </if>
        <if test="otherUEmail != null and otherUEmail != ''">
            AND OTHER_U_EMAIL LIKE CONCAT('%', #{otherUEmail}, '%')
        </if>
        <if test="messageContent != null and messageContent != ''">
            AND MESSAGE_CONTENT LIKE CONCAT('%', #{messageContent}, '%')
        </if>
        <if test="type != null and type != ''">
            AND TYPE LIKE CONCAT('%', #{type}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findUserMessageByIds" resultMap="userMessageMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM USER_MESSAGE WHERE MESSAGE_ID IN
        <foreach collection="list" index="index" item="messageId" open="(" separator="," close=")">
            #{messageId}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertUserMessage" parameterType="map">
        INSERT INTO USER_MESSAGE (
            MESSAGE_ID, U_ID, U_EAMIL, POSTS_NO, POSTS_TITLE, OTHER_U_EMAIL, MESSAGE_CONTENT, TYPE
        ) VALUES (
            #{messageId},
            #{uId},
            #{uEamil},
            #{postsNo},
            #{postsTitle},
            #{otherUEmail},
            #{messageContent},
            #{type}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateUserMessage" parameterType="map">
        UPDATE USER_MESSAGE
        <set>
            <if test="uId != null">
                U_ID = #{uId},
            </if>
            <if test="uEamil != null">
                U_EAMIL = #{uEamil},
            </if>
            <if test="postsNo != null">
                POSTS_NO = #{postsNo},
            </if>
            <if test="postsTitle != null">
                POSTS_TITLE = #{postsTitle},
            </if>
            <if test="otherUEmail != null">
                OTHER_U_EMAIL = #{otherUEmail},
            </if>
            <if test="messageContent != null">
                MESSAGE_CONTENT = #{messageContent},
            </if>
            <if test="type != null">
                TYPE = #{type}
            </if>
        </set>
        WHERE MESSAGE_ID = #{messageId}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteUserMessageById" parameterType="int">
        DELETE FROM USER_MESSAGE WHERE MESSAGE_ID = #{messageId}
    </delete>
    
</mapper>