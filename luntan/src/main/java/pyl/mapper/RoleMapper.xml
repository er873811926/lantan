<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://www.tablego.cn
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author: bianj
    Email: tablego@qq.com
    Version: 6.0.0
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- ROLE -->
<mapper namespace="pyl.mapper.role">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="roleMap" type="Role">
        <id column="ROLE_ID" property="roleId" jdbcType="INTEGER" />
        <result column="ROLE_NAME" property="roleName" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ROLE_ID, ROLE_NAME
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findRoleAll" resultMap="roleMap">
        SELECT
        <include refid="allColumns" />
        FROM ROLE
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findRoleByCondition" resultMap="roleMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM ROLE WHERE 1 = 1
        <if test="roleName != null and roleName != ''">
            AND ROLE_NAME LIKE CONCAT('%', #{roleName}, '%')
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findRoleByIds" resultMap="roleMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM ROLE WHERE ROLE_ID IN
        <foreach collection="list" index="index" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertRole" parameterType="map">
        INSERT INTO ROLE (
            ROLE_ID, ROLE_NAME
        ) VALUES (
            #{roleId},
            #{roleName}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateRole" parameterType="map">
        UPDATE ROLE
        <set>
            <if test="roleName != null">
                ROLE_NAME = #{roleName}
            </if>
        </set>
        WHERE ROLE_ID = #{roleId}
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteRoleById" parameterType="int">
        DELETE FROM ROLE WHERE ROLE_ID = #{roleId}
    </delete>
    
</mapper>